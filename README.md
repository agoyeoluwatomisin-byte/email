# Email Sender

A small Next.js and Supabase email sender scaffold.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

Apply `supabase/schema.sql` to your Supabase project and configure the values in `.env.local`.
