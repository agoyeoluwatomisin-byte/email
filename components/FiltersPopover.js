export default function FiltersPopover({ filters, onChange, onApply, onClear, onClose }) {
  return <div className="mail-filter-popover" role="dialog" aria-label="Message filters"><div className="mail-filter-grid"><label>From date<input className="mail-input" type="date" value={filters.dateFrom} onChange={(event) => onChange({ ...filters, dateFrom: event.target.value })} /></label><label>To date<input className="mail-input" type="date" value={filters.dateTo} onChange={(event) => onChange({ ...filters, dateTo: event.target.value })} /></label><label>Sender domain<input className="mail-input" value={filters.senderDomain} onChange={(event) => onChange({ ...filters, senderDomain: event.target.value })} placeholder="example.com" /></label><label>Read state<select className="mail-input" value={filters.read} onChange={(event) => onChange({ ...filters, read: event.target.value })}><option value="">All messages</option><option value="false">Unread</option><option value="true">Read</option></select></label><div className="mail-composer-row"><button className="mail-button mail-button-primary" type="button" onClick={() => { onApply(); onClose(); }}>Apply</button><button className="mail-button" type="button" onClick={() => { onClear(); onClose(); }}>Clear</button></div></div></div>;
}
