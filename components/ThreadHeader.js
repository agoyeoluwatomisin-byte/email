import MailIcon from './MailIcon';

export default function ThreadHeader({ subject, label, status, claimedBy, activityCount, onBack, onAction, onStatusChange, canRelease }) {
  return <header className="mail-thread-header"><button className="mail-icon-button mail-mobile-only" type="button" onClick={onBack} aria-label="Back to message list"><MailIcon name="back" /></button><div className="mail-thread-heading"><h1>{subject || '(no subject)'}</h1><div className="mail-thread-meta"><span className="mail-badge">{label || 'other'}</span><span className="mail-badge">{status || 'new'}</span><span className="mail-badge">{claimedBy ? `Claimed by ${claimedBy}` : 'Unclaimed'}</span><span className="mail-badge">{activityCount} activity</span></div></div><div className="mail-thread-tools"><button className="mail-icon-button" type="button" onClick={() => onAction('archive')} aria-label="Archive thread" title="Archive"><MailIcon name="archive" /></button><button className="mail-icon-button" type="button" onClick={() => onAction('delete')} aria-label="Delete thread" title="Delete"><MailIcon name="trash" /></button>{canRelease && <button className="mail-button" type="button" onClick={() => onAction('release')}>Release</button>}<select className="mail-input mail-status-select" aria-label="Thread status" value={status || 'new'} onChange={(event) => onStatusChange(event.target.value)}><option value="new">New</option><option value="in progress">In progress</option><option value="closed">Closed</option></select></div></header>;
}
